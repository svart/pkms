# pkms path

Find the shortest graph path between two notes.

```bash
pkms path <from> <to>
pkms --output-format json path <from> <to>
```

Targets can be UUIDs, paths, or note titles. Use this to explain whether two
topics are connected through existing links and which intermediate notes form
the connection.
